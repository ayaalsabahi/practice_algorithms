# Given a signed 32-bit integer x, return x with its digits reversed. If reversing x causes the value to go outside the signed 32-bit integer range [-231, 231 - 1], then return 0. Adapted from leetcode.

def reverseNum(n):
    n = str(n)
    reversed= n[::-1]
    
return int(revesed)
    
